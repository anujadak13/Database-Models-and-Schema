import mongoose from "mongoose"

const cate_Schema= new mongoose.Schema({
  product:{
    name: String,
    required: true
  }
}, {timestamps: true})

export const Category= mongoose.model("Category", cate_Schema)
